specVersion: "v1"
serviceId: "paynet"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "paynet-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
